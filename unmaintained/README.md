# Unmaintained patch variants

Move a version-specific patch directory here when its ongoing upkeep ends. Preserve its files and document the last checked source baseline and the reason for retiring it.

No variants have been retired in the initial standalone patch set. The available, previously source-checked variants are listed in the [main README](../README.md).
